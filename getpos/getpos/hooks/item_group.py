import frappe
from frappe import _

def item_group_length(doc, method=None):
    pass
    # if len(doc.item_group_name) > 15:
    #     frappe.throw(_("Item Group Name cannot be more than 15 characters long."))
